#ifndef RELAY_H
#define RELAY_H

// Запускает relay-сервер на указанном порту.
// Принимает ровно два TCP-соединения и пересылает байты между ними.
// Возвращает 0 при штатном завершении, ненулевое при ошибке.
int relay_run(const char *port);

#endif // RELAY_H
